# `v0.6.0` Post-Publication Verification

**State:** Complete

**Owner/purpose:** this document is the post-publish verification record for
the immutable `v0.6.0` release. [`v0.6.0.json`](v0.6.0.json) is the
machine-checked trust anchor for the release's tag/commit/tree and
crate/SBOM checksums (read by `xtask` and `release-draft.yml`'s recovery
path); this document does not duplicate those values beyond what is needed
to state a result; it records the independent checks that manifest cannot
express: registry/docs.rs live state, a clean external consumer, a
PostgreSQL release smoke, and attestation-subject verification. It closes
the exit criteria [`m6-oxide-batch-test-bootstrap.md`](../m6-oxide-batch-test-bootstrap.md)
required before `Issue #190` can be considered for closure.

## Release identity (independently re-derived, not copied)

- tag `v0.6.0`, annotated tag object `d23955f56c48dcce089203330b5999a36ceb2029`
  (`git cat-file -p v0.6.0`)
- tagged commit `e9ce3891a9c37959ad1022a62dbf723c9edd2d65`, tagged tree
  `81746a7560a1465a5f8d79655e0282e60d7d3d12` (`git cat-file -p <commit>`)
- GitHub Release id `379278864`, name `OxideBatch 0.6.0`, `draft:false`,
  `prerelease:false`, `publishedAt: 2026-08-31T03:11:29Z`
  (`gh release view v0.6.0 --json id,name,isDraft,isPrerelease,publishedAt,tagName`)
- release-event workflow run `33353036930` (`Release` / `release.yml`,
  event `release`, head SHA `e9ce3891a9c37959ad1022a62dbf723c9edd2d65`,
  conclusion `success`) (`gh run view 33353036930 --json ...`)
- registered-publication dispatch run `33348113358` (event
  `workflow_dispatch`, head SHA `7281c5245d558cc9df10e9f88513383acce0e36c`,
  conclusion `success`) — dispatched from the reviewed `main` copy of
  `release.yml` (PR #216), not the tag's own stale copy, per
  `m6-oxide-batch-test-bootstrap.md`.

All four values matched the reviewed identity exactly; none were taken from
prior documentation without an independent lookup.

## 1. crates.io state (independent, via `crates.io` API, not copied from `v0.6.0.json`)

Checked `GET https://crates.io/api/v1/crates/<crate>/0.6.0` for all six
crates on 2026-08-31. Every version: `yanked: false`, `rust_version: "1.95"`.

| Crate | `checksum` (live) | Matches manifest |
| --- | --- | --- |
| `oxide-batch-core` | `3b63311721adf30b20ef5293a2ee9fe1759211464a833295e6d490fda8d0c631` | yes |
| `oxide-batch-repository` | `cb2e0f79a4387331f7a78a4a3d9ebfcd0e7eb02856ea3df4e7a11ae9d62dbe79` | yes |
| `oxide-batch-plan` | `8cbeeac623fdc091dd12ee81d1d2a5bb6b1edba3522f5cade85960179ebf1c49` | yes |
| `oxide-batch` | `eb44a43551fbf5c70d11cc54d4c14ec8a40ebaf917a1d5ff1a158556e1fe093b` | yes |
| `oxide-batch-cli` | `5a819b8c97926ed6b1c810f93ebe1985f0c2b4a94e253414b2c7ceec656ec2ef` | yes |
| `oxide-batch-test` | `8bad1e49c191f276402c7092b441c2a38dc33ff57b49d5911c801eafa42cb49e` | yes |

`GET .../oxide-batch-test/0.6.0/dependencies` confirms the normal dependency
`oxide-batch =0.6.0` (exact pin), alongside `futures-executor`,
`serde_json`, `sqlx`, and `tokio` as `dev` dependencies (not part of the
published dependency graph).

`published_by` is `null` (Trusted Publishing/OIDC) for the five crates
published by the `publish-registered` dispatch, and the authenticated user
`luceat-lux-vestra` for `oxide-batch-test` (the one-time manual bootstrap
token publish), consistent with the bootstrap sequence.

## 2. docs.rs state

`GET https://docs.rs/crate/<crate>/0.6.0/status.json` for all six crates on
2026-08-31: every crate returned `{"doc_status":true,"version":"0.6.0"}`
(HTTP 200). No crate is failed or still building; no docs.rs source fix is
required.

## 3. Clean external consumer

- Toolchain: `rustc 1.97.1`, `cargo 1.97.1` (release MSRV is `1.95`; this is
  a newer compatible toolchain, not the release's own CI toolchain).
- Project created with `cargo new` outside this repository's working tree
  (no ancestor `Cargo.toml`, confirmed), under a fresh, isolated `CARGO_HOME`
  so no pre-existing registry cache or credential could mask a repository
  path/workspace dependency.
- `Cargo.toml`:
  ```toml
  [dependencies]
  oxide-batch = "=0.6.0"
  ```
- `cargo generate-lockfile` (fresh `CARGO_HOME`) locked
  `oxide-batch 0.6.0` with `source = "registry+https://github.com/rust-lang/crates.io-index"`
  and `checksum = "eb44a43551fbf5c70d11cc54d4c14ec8a40ebaf917a1d5ff1a158556e1fe093b"`
  — proving resolution against the public registry, not a repository
  checkout or patched source.
- `cargo check --locked` and `cargo run --locked` both succeeded, exercising
  the documented minimal public workflow from the facade's own crate-level
  doc example (`JobName`/`JobParameters`/`JobInstanceKey`):
  ```
  oxide-batch 0.6.0 consumer smoke: OK, key = JobInstanceKey { job_name: JobName("daily_import"), identifying_parameter_count: 1, .. }
  ```

Result: **PASS**.

## 4. Supported PostgreSQL release smoke

- PostgreSQL: `18.6` (Homebrew, `aarch64-apple-darwin`), one of the two
  release-blocking M2/M6 majors (15 and 18) per
  [`postgres-setup.md`](../../operations/postgres-setup.md); the server
  runs an isolated scratch database (`oxide_batch_190_release_smoke`), not
  a shared cluster.
- Dependency source: the same clean-consumer project, extended with
  `oxide-batch = { version = "=0.6.0", features = ["postgres"] }` and
  `oxide-batch-test = { version = "=0.6.0", features = ["postgres"] }`,
  re-locked from the fresh `CARGO_HOME`; `Cargo.lock` confirms
  `oxide-batch-test`'s own `source`/`checksum` also resolve to the published
  crates.io `registry+...` entry, matching the digest in section 1.
- Scenario: `oxide-batch-test`'s own public-API restart evidence
  (`postgres_multi_resource_restart.rs`, copied verbatim as an external
  test — it imports only `oxide_batch::` and `oxide_batch_test::` public
  items) run against the published crates:
  `cargo test --test postgres_release_smoke --features postgres --locked`
  with `OXIDEBATCH_POSTGRES_TEST_URL` set to the scratch database.
  - `multi_resource_reader_restarts_across_a_resource_boundary_crash`:
    repository initialization (`PostgresFixture::migrate`/`connect`),
    a chunk-transaction commit of two chunks, an injected stop before the
    resource-boundary transition, and a second attempt that resumes from
    the durably committed checkpoint and completes — proving job execution
    persistence, restart/resume, and repository state recovery.
  - `multi_resource_writer_restarts_across_a_resource_boundary_crash`: the
    writer-side analog, same transaction/rollback and resume semantics.
  - `multi_resource_reader_completes_across_a_resource_boundary_with_no_crash`:
    a same-attempt completion baseline.
  - All three: `ok` (`test result: ok. 3 passed; 0 failed`).

Result: **PASS**.

## 5. Release checksum manifest (`oxide-batch-0.6.0.sha256`)

Downloaded every GitHub Release asset with `gh release download v0.6.0` and
independently computed `shasum -a 256` over each `.crate` and `.cdx.json`
file and over the manifest file itself:

- manifest file `sha256`: `163c878b9a6ce21660a6257f435f5e05775f86d2e1bb9a7618553a5ee42b653a`
  — matches the reviewed value.
- every `.crate` and `.cdx.json` checksum listed inside the manifest matched
  both the independently recomputed digest of the downloaded bytes and the
  live crates.io `checksum` field from section 1. No stale or missing crate
  entry.

## 6. SBOM artifacts

The six `.cdx.json` CycloneDX SBOM assets downloaded in section 5, hashed
independently:

| Crate | SBOM `sha256` (independently recomputed) |
| --- | --- |
| `oxide-batch-core` | `7dc67d5f9f5e91ede4a3b55ae18f97dc98d29eac76a99c597c195f13c94eb9b5` |
| `oxide-batch-repository` | `fe8c35c63fa2e7edd4b518d907a716ab0fcc4b1b112803b25af7be7a1968434f` |
| `oxide-batch-plan` | `91debc386ff552149ce7d3bbd458401faf346a144a86ca89bf81359ee82ef996` |
| `oxide-batch` | `c17f39c67eba56f3b6e418e08436831a2a9d1cb3da98948ba58f3ccb649f03ce` |
| `oxide-batch-cli` | `e0c32a135aaac49d45defd8be21e2fc588c729962b4cd1523b5213edfd5609e2` |
| `oxide-batch-test` | `f36c41cbbb9655b74e5689e69167ecf27d6b5508d0b9dbdd5cb07d3e254a1446` |

All six match [`v0.6.0.json`](v0.6.0.json) exactly. No SBOM was regenerated
or substituted; these are the already-attested release assets.

## 7. Provenance / attestations

`gh api repos/luceat-lux-vestra/oxide-batch/attestations/sha256:8bad1e49c191f276402c7092b441c2a38dc33ff57b49d5911c801eafa42cb49e`
(the `oxide-batch-test` `.crate` digest) returned two attestations. Decoding
each DSSE envelope's payload independently (not trusting attestation
existence alone):

1. `predicateType: https://cyclonedx.org/bom`, subject
   `oxide-batch-test-0.6.0.crate` digest `sha256:8bad1e49...` — the SBOM
   attestation #212/#213 recovered. Its certificate embeds
   `workflow_dispatch`, commit `c2641e5baf559eba8ec9df28d0670f689bcb458c`,
   and `actions/runs/33342438324` — matching recovery run `33342438324`
   (`gh run view 33342438324`: `Prepare release draft`, `workflow_dispatch`,
   head SHA `c2641e5...`, conclusion `success`). The attestation's
   `bundle_url` blob path ends `.../44016882.json.sn`, matching the
   recorded attestation id `44016882`.
2. `predicateType: https://slsa.dev/provenance/v1`, subjects: all six
   `<crate>-0.6.0.crate` names with digests matching section 1/5 exactly —
   the build-provenance attestation covering the whole release set.

Subject identity, not just attestation existence, was verified in both
cases.

## Configuration that remains owner/user-confirmed, not independently verifiable

crates.io does not expose a public read API for Trusted Publisher
configuration. The Trusted Publisher for `oxide-batch-test`
(owner `luceat-lux-vestra`, repository `oxide-batch`, workflow
`release.yml`, environment `release`) is recorded as **owner-confirmed**,
not independently API-verified. The indirect evidence is consistent with
it: `published_by: null` (OIDC) for the five crates published by the
`publish-registered` dispatch, versus an authenticated user for the one
manually bootstrapped `oxide-batch-test` publish (section 1).

## Result

All eight checks above passed. No docs.rs failure, no checksum mismatch, no
attestation-subject mismatch, and no crate is yanked. Combined with the
already-merged bootstrap/publication history recorded in
[`m6-oxide-batch-test-bootstrap.md`](../m6-oxide-batch-test-bootstrap.md),
this satisfies the post-publish verification requirements that
[Issue #190](https://github.com/luceat-lux-vestra/oxide-batch/issues/190)
gates on before it may be closed.

## Explicitly out of scope for this document

Promoting any M6 compatibility-ledger row (`support-matrix.md`,
`conformance-matrix.md`) from "campaign-evidenced" to released `Verified`
status is a separate governance decision this document does not make. The
checks above establish that the named release and post-publication evidence
now exist — a precondition for such a promotion — but do not themselves
decide it.

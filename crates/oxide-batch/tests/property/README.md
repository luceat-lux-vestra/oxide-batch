# Property tests

Lifecycle state-machine and job-instance identity properties live here. Every
generated case must use the seeded generator from `tests/support/` and include
the seed plus captured events in failure diagnostics.
